---
title: How Web Search works
pcx_content_type: concept
description: Understand Web Search's discovery-only design, the search-then-fetch workflow, and how it differs from AI Search.
sidebar:
  order: 3
  badge: Beta
head:
  - tag: title
    content: How Web Search works
---

Web Search is a **discovery** API. It takes a query and returns a list of URLs that match, along with metadata like titles and descriptions. It does not return page content.

This design is intentional. Discovery and content retrieval are separate steps, which gives you full control over what you fetch and how you process it.

## The search-then-fetch workflow

A typical workflow with Web Search has two steps:

1. **Search** (Web Search): find relevant URLs for a query
2. **Fetch** (Browser Run or `fetch()`): get the content of the pages you want

```
Query → Web Search → URLs + metadata
                         ↓
                    Pick best results
                         ↓
              Browser Run / fetch() → Full page content
                         ↓
                    Process content
```

For example, an agent researching a topic would:

1. Call `env.WEBSEARCH.search({ query: "topic" })` to discover relevant pages
2. Evaluate the returned titles and descriptions to decide which pages are worth reading
3. Fetch content from selected URLs using [Browser Run](/browser-run/) (for rendered HTML, markdown, structured data) or `fetch()` (for simple HTTP requests)
4. Process the content with an LLM or extract specific data

This composable approach means you choose the right tool for content retrieval. Use [Browser Run /markdown](/browser-run/quick-actions/markdown-endpoint/) for clean text, [/json](/browser-run/quick-actions/json-endpoint/) for structured extraction, or [/screenshot](/browser-run/quick-actions/screenshot-endpoint/) for visual captures.

## What is in a search result

Each result contains metadata about the page, not the page content itself:

| Field | Description |
| --- | --- |
| `url` | The canonical URL of the page |
| `title` | The page title |
| `description` | Page-level description (from the page's meta tags), if available |
| `summary` | A summary of the page contents, if available |
| `authors` | Author names associated with the page |
| `imageUrl` | Preview image URL (OpenGraph or similar) |
| `faviconUrl` | Favicon URL for the page's domain |
| `lastModifiedDate` | When the page was last modified (ISO-8601) |

This metadata is enough for an agent to assess relevance and decide which pages to fetch.

## Web Search vs AI Search

Web Search and [AI Search](/ai-search/) serve different purposes:

| | Web Search | AI Search |
| --- | --- | --- |
| **What it searches** | The public web | Your own data |
| **Setup** | Zero-setup, one binding | Create an instance, index your content |
| **Use case** | Find information on the open internet | Search over documents you control |
| **Content** | Returns URLs (you fetch content separately) | Returns indexed content directly |

Use Web Search when your agent needs real-time information from across the web. Use AI Search when you need retrieval over your own documents, knowledge base, or application data.
